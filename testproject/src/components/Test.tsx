import * as Small from "smalljsx"

export interface Option {
    name: string;
    correct: string;
    options: {
        
    };
}

export interface TestContents {
    options: 
}

export const TestContext = Small.createContext();

export const Test: Small.ParentComponent = () => {

};